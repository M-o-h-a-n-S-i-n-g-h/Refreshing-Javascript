print();

function print() {
  console.log("I'm Hoisted by the Javascript Interpreter");
}

// Hoisting - When Javscript interpreter interprets the function keyword, the function gets hoisted and it moves the function declaration to the top level of the file and makes a reference in the memory
